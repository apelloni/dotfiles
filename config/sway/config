# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#


# Get input devices with swaymsg -t get_input
set $TOUCHPAD "1267:12792:ELAN067B:00_04F3:31F8_Touchpad"
set $MOUSE "1133:45082:MX_Anywhere_2S_Mouse"
input $TOUCHPAD {
    natural_scroll enable
    middle_emulation true # kills middle button on trackpad
}
input $MOUSE {
    natural_scroll enable
}
# Keyboard Layout us <-> de toggle
exec_always "setxkbmap -layout 'us,ch(de)'"
exec_always "setxkbmap -option 'grp:alt_shift_toggle'"

set $sway-config-directory ~/.config/sway/
set $sway-scripts-directory ~/.config/sway/scripts/

# Set Background
exec_always swaybg -i '/home/andrea/Pictures/2015-KLIMT-04-FEAT.jpg'

# Remove screen saver
exec xset s off
# Set screen off after 20 minutes
exec xset dpms 0 0 1200

# Conky
exec_always ~/.config/conky/Mesarthim/start.sh

###################
# Functionality

# Allow transparency
#exec_always picom --conf ~/.config/picom.conf
# Notifications
exec_always dunst -conf $sway-config-directory/dunstrc1
# Battery
exec $sway-scripts-directory/i3-battery-popup -n -l15 -L20
# udisk2 manager
exec udiskie

###################

# Import GTK themes
exec_always $sway-scripts-directory/import_gsettings

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
# exec dex --autostart --environment i3

# use swayidle to active swaylock
exec swayidle -w \
	before-sleep 'loginctl lock-session $XDG_SESSION_ID' \
	lock 'playerctl -a pause; swaylock --config $sway-config-directory/swaylock.conf'

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec nm-applet

# Use pactl to adjust volume in PulseAudio.
bindsym XF86AudioLowerVolume exec $sway-scripts-directory/volume-control.sh down
bindsym XF86AudioRaiseVolume exec $sway-scripts-directory/volume-control.sh up
bindsym XF86AudioMute exec $sway-scripts-directory/volume-control.sh mute
bindsym XF86AudioMicMute exec $sway-scripts-directory/volume-control.sh mute-input

# Use brightnessctl to adjust brightness
bindsym XF86MonBrightnessUp exec $sway-scripts-directory/brightness-control.sh up
bindsym XF86MonBrightnessDown exec $sway-scripts-directory/brightness-control.sh down

# warm/cold brightness
exec wl-gammarelay
bindsym $mod+Ctrl+1 exec busctl --user -- set-property rs.wl-gammarelay / rs.wl.gammarelay Temperature q 6500
bindsym $mod+Ctrl+2 exec busctl --user -- set-property rs.wl-gammarelay / rs.wl.gammarelay Temperature q 4500
bindsym $mod+Ctrl+3 exec busctl --user -- set-property rs.wl-gammarelay / rs.wl.gammarelay Temperature q 2500

# Capture print of screen
#bindsym Print exec XDG_CURRENT_DESKTOP=sway flameshot
bindsym Print exec grim -g "`slurp`" - | wl-copy --type image/png \
    && wl-paste > ~/Pictures/screenshot_`date +%Y%m%d_%H%M%S`.png \
    && notify-send -i clipboard "Screen Shot" "Area captured and saved in clipboard\n screenshot-`date +%Y%m%d_%H%M%S`.png"

# Monitors
# To Check which are the monitor one can use xrandr
set $monitor-default eDP-1
set $monitor-external "(HDMI-1|DP-[1-9])"
#bindsym XF86Display exec sh $sway-scripts-directory/screen-switch.sh $monitor-default $monitor-external
#bindsym $mod+F7 exec sh $sway-scripts-directory/screen-switch.sh $monitor-default $monitor-external

# Disable laptop when lid is closed
set $laptop $monitor-default
bindswitch --reload --locked lid:on output $laptop disable
bindswitch --reload --locked lid:off output $laptop enable


# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec kitty

# kill focused window
bindsym $mod+q kill

# Autotiling
#exec_always i3-auto-tiling
exec_always autotiling -w 1 2 3 4 5 6 7 8 9 0

# start rofi (a program launcher)
#bindsym $mod+d exec rofi -i -show drun -modi drun -show-icons
bindsym $mod+d exec ~/.config/rofi/launchers/colorful/launcher.sh
bindsym $mod+Shift+d exec ~/.config/rofi/launchers/colorful/windows.sh
#bindsym $mod+Shift+d exec rofi -show window
bindsym $mod+n exec $sway-scripts-directory/wifi-menu
bindsym $mod+b exec $sway-scripts-directory/rofi-bluetooth
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec i3-dmenu-desktop

# focuse on mouse hover
focus_follows_mouse no

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left 20 px
bindsym $mod+Shift+Down move down 20 px
bindsym $mod+Shift+Up move up 20 px
bindsym $mod+Shift+Right move right 20 px
# small movement
bindsym Ctrl+Shift+Left move left 2 px
bindsym Ctrl+Shift+Down move down 2 px
bindsym Ctrl+Shift+Up move up 2 px
bindsym Ctrl+Shift+Right move right 2 px

# center window
bindsym $mod+c move position center

# split in horizontal orientation
#bindsym $mod+h split h

# split in vertical orientation
#bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym Alt+Tab focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# workspace dispaly output
workspace 1 output primary
workspace 2 output primary
workspace 3 output primary
workspace 4 output primary
workspace 5 output eDP-1 primary
workspace 6 output primary
workspace 7 output primary
workspace 8 output eDP-1 primary
workspace 9 output eDP-1 primary
workspace 10 output eDP-1 primary

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+e exec ~/.config/rofi/powermenu/powermenu.sh

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode
        bindsym $mod+c move position center
        bindsym $mod+Shift+1 resize set width 50 ppt;\
                       resize set height 90 ppt;\
                       move position center;\

        bindsym $mod+Shift+2 resize set width 80 ppt;\
                            resize set height 90 ppt;\
                            move position center;\

        bindsym $mod+Shift+3 resize set width 50 ppt;\
                            resize set height 50 ppt;\
                            move position center;\

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # small movement
        bindsym Shift+Left resize shrink width 2 px or 2 ppt
        bindsym Shift+Down resize grow height 2 px or 2 ppt
        bindsym Shift+Up resize shrink height 2 px or 2 ppt
        bindsym Shift+Right resize grow width 2 px or 2 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

#polybar
#exec_always ~/.config/polybar/polybar.sh &
exec_always $sway-config-directory/waybar.sh


set $gap 3
gaps inner $gap
gaps outer $gap
gaps horizontal $gap
gaps vertical $gap
gaps top 2
gaps bottom $gap
gaps right $gap
gaps left $gap

# Fix windows workspace
for_window [app_id="firefox"] move container to workspace $ws1
for_window [instance="Mail"] move container to workspace $ws5
for_window [class="[Zz]oom"] move container to workspace $ws6
for_window [app_id="^join\?action=join.*$"] move container to workspace $ws6
for_window [class="[Zz]ulip"] move container to workspace $ws8
for_window [class="[Ss]lack"] move container to workspace $ws9
for_window [class="[Ee]lement"] move container to workspace $ws9
for_window [class="^[Bb]etterbird$"] move container to workspace $ws5
for_window [app_id="^[Tt]askade$"] move container to workspace $ws7

for_window [app_id="chess_board"] floating enable
#for_window [app_id="nannou"] floating enable

# Pulseaudio Pavucontrol window
for_window [app_id="[Pp]avucontrol"] floating enable
for_window [app_id="[Pp]avucontrol"] resize set width 50 ppt
for_window [app_id="[Pp]avucontrol"] resize set width 50 ppt
for_window [app_id="[Pp]avucontrol"] move position center



# Define floating windows
# zoom also for meetings joined via a link
for_window [app_id="[Cc]alculator$"] floating enable
for_window [app_id="[Ss]hutter$"] floating enable
for_window [title="^zoom\s?$"] floating enable
for_window [title="^join\?action=join.*$"] floating enable
for_window [app_id="^[Ss]ignal$"] floating enable
for_window [app_id="^[Ee]volution$"] floating enable
for_window [app_id="^[Bb]lue[Mm]ail$"] floating enable
for_window [app_id="^[Ss]pectacle$"] floating enable


## BORDERS
for_window [app_id="^.*"] border pixel 5
# class                 border  backgr. text    indicator child_border
#client.focused         #4C7899 #285577 #FFFFFF #FF9EF4   #FF9EF4
client.focused          #4C7899 #285577 #FFFFFF #3A98B9   #3A98B9
client.focused_inactive #333333 #5F676A #FFFFFF #484E5000 #5F676A00
client.unfocused        #333333 #222222 #888888 #292D2E   #222222
client.urgent           #2F343A #900000 #FFFFFF #900000   #900000
client.placeholder      #000000 #0C0C0C #FFFFFF #000000   #0C0C0C

client.background       #FFFFFF

# Autostart
#exec i3-msg "workspace 5; append_layout ~/.config/i3/workspace_layout/workspace-5.json"
exec i3-msg "workspace $ws10; exec kitty --session $sway-scripts-directory/kitty.setup"
exec i3-msg "workspace $ws2; exec kitty"
exec i3-msg "workspace $ws2; exec kitty"

#exec gnome-calendar
#exec gnome-weather
#exec taskade
exec slack
exec element-desktop
exec zulip
exec firefox
#exec brave
exec dropbox
exec betterbird
#exec thunderbird



## Start i3bar to display a workspace bar (plus the system information i3status
## finds out, if available)
#bar {
#        status_command i3status
#}
